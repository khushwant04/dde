# Example only. Copy to secret.yaml, replace the placeholder locally, and never commit it.
apiVersion: v1
kind: Secret
metadata:
  name: dde-provider
  labels:
    app.kubernetes.io/name: dde
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: document-data-extractor
  annotations:
    dde.dev/example-only: "true"
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_ME"
